node_modules/
dist/
.turbo/
.vercel/
coverage/
*.log
.env
.env.*
!.env.example
# Demo builds need VITE_DEMO=true at build time, on CI too. No secrets in these.
!apps/*/.env.demo
.DS_Store
dist-demo/

# Next.js site: build output, and the demo builds copied in before each build
.next/
next-env.d.ts
*.tsbuildinfo
apps/site/public/play/

# Compliance source snapshots (third-party documents, local reference only)
docs/compliance/sources/*.txt
