# Required when deployed. Upstash REST credentials (Vercel Marketplace sets KV_REST_API_* too).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Random 32+ byte secret used to sign session tokens.
SESSION_SECRET=
# Optional comma-separated list of allowed browser origins. Empty allows all.
ALLOWED_ORIGINS=
