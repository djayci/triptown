/// <reference types="vite/client" />

/** Injected by vite `define`; shown in the rules panel so a lab can match it to the signed manifest. */
declare const __APP_VERSION__: string;
declare const __BUILD_HASH__: string;
