## 1. Gate

- [x] 1.1 Write a marketing-stage compliance note at `docs/compliance/games-showcase-site-<date>.md`, using the `game-compliance-audit` skill's `references/jurisdictions.md`. Cover the site as an advert (ARCON Act s.54/s.63, UK CAP 16.3.12 and the Oct 2025 under-18 guidance, AGCO 2.03, Brazil 1.231), free play as advertising, the session-cookie self-declaration gate, and the Whack Crash adult skin (cute moles, "before it dives"), including whether Whack Crash may be `live` on the site until that is fixed. Verify the note exists and its required fixes are folded into groups 3–5 before copy is final

## 2. Scaffold

- [x] 2.1 Create `@triptown/site` in `apps/site` as a Next.js 16 App Router app: `package.json` (`dev` on port 5180, `prebuild`, `build`, `start`, `lint`, `typecheck`, `test`, `assemble`, `capture`), `next.config.ts`, `tsconfig.json`, `app/layout.tsx`, `app/page.tsx`, `app/globals.css`, with `@triptown/core`, `tsx` and `vitest` as dev dependencies; add `apps/site/public/play/` and `apps/site/.next/` to `.gitignore`. Verify `pnpm install` succeeds and `pnpm --filter @triptown/site typecheck lint build` pass on the empty app (record here any TypeScript or ESLint pin that was needed, design D10). Recorded 17 Sep 2026: no TypeScript pin needed (Next 16.3.5 typechecks under the repo's TS ~6.0); `@next/eslint-plugin-next` had to go in the root devDependencies, because the shared `eslint.config.js` is loaded from the root by every package's lint; `typecheck` runs `next typegen` first so `next-env.d.ts` (gitignored) exists
- [x] 2.2 Add `src/catalogue.ts` (design D5) with Whack Crash and Gate Rush `live` with an empty `demoQuery` (the games as built; the first version used `regulated-uk` and `skin: 'adult'` until the user's 17 Sep 2026 decision), The Cable Car `in-development`, and `playUrl(entry)` returning `/play/<slug>/index.html?<query>` for live entries and `null` otherwise. Verify unit tests cover unique slugs, query encoding, the `index.html` path and the `in-development` case
- [x] 2.3 Add `src/copy.ts` holding every visible string (hero line, "Triptown, proudly powered by Triptych", B2B and play-money statements, gate question, exit message, footer, row labels). Verify a test asserts no string literal for visible copy remains in `app/**/*.tsx` other than imports from `copy.ts` and the catalogue

## 3. The 18+ gate

- [x] 3.1 Write `src/gate.ts`: the cookie name and attributes (session, `HttpOnly`, `Secure`, `SameSite=Lax`, `Path=/`), `isConfirmed(cookies)`, and `safeNext(value)` that accepts only paths starting `/play/` with no `//`, `\` or scheme. Verify unit tests cover accepted paths, `//evil.com`, `/play/../x`, `https://…`, `javascript:` and an empty value
- [x] 3.2 Write `app/actions.ts` with `confirmAge(formData)` (sets the cookie, redirects to `safeNext(next)` or `/#games`) and `declineAge()` (sets nothing, redirects to `/?declined=1`). Verify tests with a mocked cookie store assert the cookie has no `Max-Age`/`Expires` and that decline writes no cookie
- [x] 3.3 Write `proxy.ts` with `matcher: ['/play/:path*']`: no cookie → 307 to `/?next=<path+search>`; cookie → pass. Verify unit tests on the proxy function for `/play/gate/index.html?profile=ng-draft&skin=adult` and `/play/gate/assets/atlas-adult.json`, with and without the cookie
- [x] 3.4 Render the gate in `app/page.tsx` as a server component: unconfirmed → wordmark, Triptych line, B2B statement, the two form buttons carrying `next` through a hidden field, and the games greyed out with no links (changed from hidden at the user's request, 17 Sep 2026); `?declined=1` → exit message; confirmed → the catalogue. Verify a test renders the page for each state and asserts the unconfirmed and declined HTML contain no `/play/` link or other `/play/` substring (apart from the visitor's own return path in the hidden `next` field), no Play link, and the live rows marked locked

## 4. Page

- [x] 4.1 Build the hero from the Black Glass board (design D8): nav-only header, chrome TRIPTOWN wordmark with red/blue fringe via `next/font` Michroma, the rule line, "Crash games for regulated operators.", and "Triptown, proudly powered by Triptych" linking to https://triptych-studio.com/. Verify a 1440×900 screenshot under `docs/compliance/evidence/<date>/site/` against the canvas board
- [x] 4.2 Add the VHS distortion: two `aria-hidden` band copies animating for ~330 ms in each 5 s cycle (8 s originally), fringe widening in step, all removed under `prefers-reduced-motion`. Verify a Playwright script samples the wordmark's bounding box every 50 ms for 20 s and finds it unchanged, finds the band copies visible for under 400 ms per cycle, at most 5 times and at least 4 s apart, and finds no running animation with `reducedMotion: 'reduce'`
- [x] 4.3 Build the game rows (design D8): accent spine, 16:9 logo tile in the game's look with an accessible name (design D7), name and pitch, fixed 240 px Play column with accessible name "Play <name> demo", "NO SIGNAL" / "COMING SOON" for in-development entries; the play-money statement above the first row; the footer with "Proudly powered by Triptych". Verify screenshots at 390×844 and 1440×900 show no horizontal scroll (`scrollWidth <= innerWidth`), the statement above the first row, and Play-column dividers at the same x on every row at 1440×900
- [x] 4.4 Write the pitches and page copy under the spec's "No promotional gambling claims" requirement and the note from 1.1. Verify 5.2's copy check passes

## 5. Build: assemble, check, tiles

- [x] 5.1 Add `build:demo` to `turbo.json` (outputs `dist-demo/**`) and make `@triptown/site#build` depend on `@triptown/whack#build:demo` and `@triptown/gate#build:demo`; write `scripts/assemble-demos.ts` copying each live entry's `dist-demo` into `public/play/<slug>/` after clearing it, and add `next.config.ts` redirects `/play/:slug` and `/play/:slug/` → `/play/:slug/index.html` plus `X-Robots-Tag: noindex, nofollow` on every route until an ARCON route is chosen (note F2). Verify `pnpm turbo run build --filter=@triptown/site` produces `public/play/whack/index.html` and `public/play/gate/index.html` and no `public/play/cable-car/`
- [x] 5.2 Write `scripts/check-site.ts` (design D6) and run it in `prebuild` after assembly. Verify a Vitest fixture test fails once per rule (duplicate slug, missing demo, missing preview, each banned-wording category including success/wealth terms (note F3), wrong Triptych URL) naming the entry or phrase, and passes on the real catalogue
- [x] 5.3 Draw each live game's logo tile in its own look (design D7), replacing the screenshot previews first built here. Verify at 1440×900 and 390×844 that the Whack Crash and Gate Rush stickers fit inside their tiles, match the HUD logo words and colours, and that the build fails a live entry with no tile theme (checks test)

## 6. End-to-end and deploy

- [x] 6.1 Write `scripts/e2e-gate.ts` against `next start`: (a) a fresh context requesting `/play/gate/index.html` and `/play/gate/assets/<a real hashed file>` gets a 307 to `/?next=…` with none of the file's bytes; (b) the home page HTML has no `/play/` link; (c) tapping NO leaves no cookie and no link; (d) keyboard Tab/Enter on "YES, 18+" lands back on the requested demo, which reaches its betting screen with the DEMO label and the adult atlas requested; (e) the confirmed home page made no `/play/` request; (f) a new context (new session) is gated again; (g) no demo request goes to another origin. Verify the script passes and fails when the proxy matcher is temporarily removed. Recorded 17 Sep 2026: 28/28 pass on `next start`; with the matcher replaced by `/__disabled/:path*` 8 checks fail (both files served 200 without the cookie). The DEMO label is drawn inside the canvas, so (d) asserts the demo build instead through its demo-only hooks, which production builds strip (`check-no-mock.mjs`). Evidence screenshots: `docs/compliance/evidence/2026-09-17/site/`
- [x] 6.2 Add `apps/site/vercel.json` (design D9), have the user create the Vercel project (Root Directory `apps/site`, outside-root files included), and deploy a preview. Verify against the preview URL with `curl -I`: `/play/gate/index.html` and one hashed asset return 307 without the cookie and 200 with `Cookie: tt_age=1`, and carry `x-robots-tag: noindex`. Record the URL here. Recorded 17 Sep 2026: Vercel project `triptown` in JC's projects (Root Directory `apps/site`, linked to github.com/djayci/triptown `main`), live at https://triptown.vercel.app. Without the cookie `/play/gate/index.html`, `/play/gate/assets/index-ADbZOu9T.js` and `/play/whack/index.html` each answer 307 to `/?next=…` with a 15-byte body; with `Cookie: tt_age=1` each answers 200; `x-robots-tag: noindex, nofollow` on `/` and `/play/`
- [x] 6.3 Update `AGENTS.md`: add `apps/site` (Next.js) to the layout and commands, the rule that no demo is served without the 18+ session cookie, and that a new game appears through a catalogue entry and a preview. Verify the text matches the implemented scripts
- [x] 6.4 Run the gate. Verify `pnpm turbo run lint typecheck test --filter=@triptown/site` passes, and `pnpm --filter @triptown/whack build` still passes `check-no-mock.mjs`
